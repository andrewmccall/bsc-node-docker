apiVersion: v1
kind: Service
metadata:
  name: {{ include "bsc-node.fullname" . }}
  labels:
    {{- include "bsc-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bsc-node.selectorLabels" . | nindent 4 }}
