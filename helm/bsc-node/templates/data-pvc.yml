# ssd-claim.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bsc-node-data
spec:
  storageClassName: premium-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 700Gi